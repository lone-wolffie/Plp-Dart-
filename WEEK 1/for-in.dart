void main() {
  // to loop through items in a list
  // only takes dart object or expression as an iterator and iterates the element one at a time.
  var numbersList = [10,20,30,40,50];

  for (var i in numbersList) {
    print(i); // prints 10 20 30 40 50
  }
  
}